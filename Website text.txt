<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>NEET UG Affordable Counselling Portal</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-slate-50 text-slate-800 font-sans min-h-screen flex flex-col justify-between">

  <header class="bg-blue-700 text-white p-4 shadow-md">
    <div class="max-w-4xl mx-auto flex justify-between items-center">
      <h1 class="text-xl font-bold tracking-wide">NEET Assist UG</h1>
      <span class="text-xs bg-blue-800 px-3 py-1 rounded-full border border-blue-400">100% Free & Transparent</span>
    </div>
  </header>

  <main class="max-w-4xl mx-auto p-4 w-full flex-grow">
    
    <div class="bg-white p-6 rounded-lg shadow-sm border border-slate-200 mb-6 text-center">
      <h2 class="text-2xl font-bold text-slate-900 mb-2">NEET College & Rank Predictor</h2>
      <p class="text-slate-600 text-sm">Enter your AIR (All India Rank) to filter target government and private medical colleges with low counseling fees.</p>
    </div>

    <div class="bg-white p-6 rounded-lg shadow-sm border border-slate-200 mb-8">
      <form id="predictorForm" class="grid grid-cols-1 md:grid-cols-3 gap-4">
        <div>
          <label class="block text-xs font-semibold text-slate-600 mb-1">ALL INDIA RANK (AIR)</label>
          <input type="number" id="airRank" placeholder="e.g. 12500" required class="w-full p-2.5 border border-slate-300 rounded-md focus:ring-2 focus:ring-blue-500 focus:outline-none" />
        </div>

        <div>
          <label class="block text-xs font-semibold text-slate-600 mb-1">CATEGORY</label>
          <select id="category" class="w-full p-2.5 border border-slate-300 rounded-md focus:ring-2 focus:ring-blue-500 focus:outline-none">
            <option value="UR">General / UR</option>
            <option value="OBC">OBC</option>
            <option value="EWS">EWS</option>
            <option value="SC">SC</option>
            <option value="ST">ST</option>
          </select>
        </div>

        <div>
          <label class="block text-xs font-semibold text-slate-600 mb-1">QUOTA TYPE</label>
          <select id="quota" class="w-full p-2.5 border border-slate-300 rounded-md focus:ring-2 focus:ring-blue-500 focus:outline-none">
            <option value="AIQ">15% All India Quota</option>
            <option value="State">85% State Quota</option>
          </select>
        </div>

        <div class="md:col-span-3 mt-2">
          <button type="submit" class="w-full bg-blue-600 hover:bg-blue-700 text-white font-semibold py-2.5 rounded-md transition duration-150">
            Predict Eligible Colleges
          </button>
        </div>
      </form>
    </div>

    <div id="resultsContainer" class="hidden">
      <h3 class="text-lg font-bold mb-4 text-slate-900">Predicted Colleges & Target Ranks</h3>
      <div class="overflow-x-auto bg-white rounded-lg shadow-sm border border-slate-200">
        <table class="w-full text-left text-sm border-collapse">
          <thead class="bg-slate-100 border-b border-slate-200">
            <tr>
              <th class="p-3 font-semibold text-slate-700">College Name</th>
              <th class="p-3 font-semibold text-slate-700">State</th>
              <th class="p-3 font-semibold text-slate-700">Category</th>
              <th class="p-3 font-semibold text-slate-700">2025 Closing Rank</th>
              <th class="p-3 font-semibold text-slate-700">Annual Fee</th>
            </tr>
          </thead>
          <tbody id="resultsTable" class="divide-y divide-slate-100">
            </tbody>
        </table>
      </div>
    </div>
  </main>

  <footer class="bg-slate-900 text-slate-400 p-4 text-center text-xs">
    <p>© 2026 NEET Assist. Built to provide affordable and accessible counselling for all students.</p>
  </footer>

  <script>
    // Sample Dataset (Replace or expand with complete MCC database JSON)
    const collegeData = [
      { name: "AIIMS, New Delhi", state: "Delhi", category: "UR", closingRank: 57, fee: "₹1,628" },
      { name: "Maulana Azad Medical College (MAMC)", state: "Delhi", category: "UR", closingRank: 85, fee: "₹4,350" },
      { name: "JIPMER, Puducherry", state: "Puducherry", category: "UR", closingRank: 277, fee: "₹12,680" },
      { name: "King George's Medical University (KGMU)", state: "Uttar Pradesh", category: "UR", closingRank: 2099, fee: "₹54,900" },
      { name: "SMS Medical College", state: "Rajasthan", category: "UR", closingRank: 2400, fee: "₹33,500" },
      { name: "Government Medical College (GMC)", state: "Chandigarh", category: "UR", closingRank: 9500, fee: "₹25,000" },
      { name: "GMC Surat", state: "Gujarat", category: "OBC", closingRank: 18500, fee: "₹25,000" },
      { name: "GMC Nagpur", state: "Maharashtra", category: "SC", closingRank: 45000, fee: "₹31,400" },
      { name: "GMC Kozhikode", state: "Kerala", category: "ST", closingRank: 75000, fee: "₹27,000" }
    ];

    document.getElementById('predictorForm').addEventListener('submit', function(e) {
      e.preventDefault();
      
      const userRank = parseInt(document.getElementById('airRank').value);
      const userCategory = document.getElementById('category').value;
      const tableBody = document.getElementById('resultsTable');
      const container = document.getElementById('resultsContainer');

      tableBody.innerHTML = '';

      // Filter logic: Colleges where closing rank is higher than or close to user rank
      const eligibleColleges = collegeData.filter(college => {
        return userRank <= (college.closingRank + 2000); 
      });

      if (eligibleColleges.length === 0) {
        tableBody.innerHTML = `<tr><td colspan="5" class="p-4 text-center text-slate-500">No matching colleges found for rank ${userRank}. Try adjusting rank/quota or state parameters.</td></tr>`;
      } else {
        eligibleColleges.forEach(col => {
          const row = `
            <tr class="hover:bg-slate-50">
              <td class="p-3 font-medium text-slate-900">${col.name}</td>
              <td class="p-3 text-slate-600">${col.state}</td>
              <td class="p-3 text-slate-600">${col.category}</td>
              <td class="p-3 font-semibold text-blue-600">${col.closingRank}</td>
              <td class="p-3 text-slate-600">${col.fee}</td>
            </tr>
          `;
          tableBody.innerHTML += row;
        });
      }

      container.classList.remove('hidden');
    });
  </script>
</body>
</html>
